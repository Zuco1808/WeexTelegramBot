[project]
name = "weex-telegram-bot"
version = "0.1.0"
description = "Telegram signal parser + (kasnije) WEEX paper-trading bot"
requires-python = ">=3.10"

[tool.pytest.ini_options]
# Omogucuje 'from weexbot import ...' u testovima bez instalacije paketa
pythonpath = ["src"]
testpaths = ["tests"]
